import React from 'react';

interface GameOverScreenProps {
  className?: string;
}

export const GameOverScreen: React.FC<GameOverScreenProps> = () => {
  return <section>Game Over Screen</section>;
};
